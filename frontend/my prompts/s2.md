Do not edit code yet.

Perform a focused border-structure and layout-architecture audit of the current Post Detail page.

The goal is to stop random styling changes and first define a clear visual grammar for borders, panels, gutters, headers, and inner subsections.

Analyze the page as a split-pane workspace with these major regions:

1. Global Drishtikone navbar
2. Sutradhar persona/action strip
3. Main workspace shell
4. Left Visual panel
5. Right Content panel
6. Panel headers
   - Visual header with Image / Annotations
   - Content header with Story / Highlights / Unconceal
7. Inner Content editor areas
   - source/persona row
   - Story Blocks
   - active editor block
   - Add Block controls
   - Compose with Sutradhar
   - Tags
   - Save/Cancel footer
8. Inner Visual areas
   - annotation stats
   - image viewport
   - image action buttons
   - visibility/eye control

Focus only on border/layout structure, not colors, typography, or final beauty.

For each region, decide:

- Should it have a visible border?
- Should it share a border with a neighboring section?
- Should it be separated by a gutter/space?
- Should it touch directly like a split-pane editor?
- Should it have rounded corners or square/internal edges?
- Should its border be structural, decorative, hover-only, or removed?
- Should it collapse or compress during editing?
- Should it participate in a resizable split-pane layout?
- Should it scroll independently or belong to the parent scroll?

Important design direction:

The page should feel like a professional split-pane creative editor, not many cards floating on a canvas.

Think of tools like LeetCode, VS Code side panels, Linear, Notion editor panes, and high-end creative software:
- large regions have clear structural boundaries
- inner tools use fewer boxes
- split panes can touch or use a precise gutter
- headers are thin and functional
- borders explain layout, not decoration
- active editing surfaces can be outlined, but not every child component should glow

Specific problems to investigate from screenshots:

1. Top chrome problem
The Drishtikone navbar and Sutradhar strip take too much vertical and visual attention. They should become compact/collapsible/low-emphasis during editing.

2. Split panel problem
Visual and Content panels feel like separate rounded cards instead of an intentional workspace. Decide whether they should:
- touch each other with a shared divider,
- be separated by a small controlled gutter,
- or sit inside one larger workspace shell.

3. Border repetition problem
The Content panel, editor block, Compose box, Tags box, source row, and tabs all have visible containers. Decide which ones should lose borders.

4. Panel header problem
Image / Annotations and Story / Highlights / Unconceal currently feel like heavy controls sitting inside bordered panel headers. Decide how panel headers should be structured.

5. Active editor border problem
The active editor has a strong orange border, which may be too loud and makes the content section feel messier. Decide what the active editing state should look like.

6. Right support rail problem
Compose and Tags currently sit in a right-side area, but they still look like bordered cards. Decide whether this support rail should have its own divider, weaker surface, or no card borders.

7. Save/Cancel footer problem
The footer should connect to the Content panel’s structure. Decide whether it is part of the content panel, sticky footer, floating action row, or bottom toolbar.

Output a document called border-layout-plan.md with:

A. Current border map
List every visible border/container currently present.

B. Proposed border grammar
Define rules such as:
- Level 0: page background
- Level 1: global shell/chrome
- Level 2: major split panes
- Level 3: panel headers
- Level 4: active editor surface
- Level 5: inline tools/chips

C. Touch vs gap rules
Decide where sections should touch, where gutters should exist, and where rounded corners should appear.

D. Editing-mode layout rules
Define what collapses, compresses, fades, or stays structural during editing.

E. Implementation plan
Split fixes into:
- P0: border/layout architecture fixes
- P1: interaction/collapse improvements
- P2: polish/material/color improvements

F. File impact
Identify likely files/classes/components to modify, but do not edit code yet.

Do not make vague recommendations.
Do not talk about making it “beautiful.”
Define the physical architecture of the interface.