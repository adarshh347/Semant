import React from 'react';
import { NavBar } from 'frontend';

// NavBar — the app's top bar: brand mark + section links + theme toggle and a
// primary "Upload" CTA. Renders inside a router (provided by the DS provider).
export const Default = () => <NavBar />;
