# R2.0 — Deferred rendered probe: **PASS**

The expression-Percept → Writer chip → image recall loop is now **rendered-verified** in a real
browser against the real backend and real Mongo. This closes R1's one unmet success criterion,
which had been honestly reported as *blocked*, not failed.

Level **A** (unsaved interaction). **Zero production mutation.**

## Environment

- Backend `uvicorn` on `127.0.0.1:8000`, frontend `vite` dev on `127.0.0.1:5173`, both from this
  worktree at the rebased rehearsal branch. R1's blocker (long-lived servers being killed) did
  not recur.
- One environment correction was required and is worth recording: `frontend/.env` sets
  `VITE_API_URL=http://localhost:8000`, so the backend must run on **8000**, not the code default
  5008. Started on 5008 first, every axios call failed, and the app hung on "Loading…". The
  user's `.env` was **not** modified — the backend was moved to the port the config expects.

## Subject (real, unchanged)

Post **`695be6c9a9ea58f1b6aef5e0`** — the five-sculpture image (Solanki · Gupta · Pala ·
Amaravati · Gandhara). Carries expression percept **`pctx_mrpi3rjk_0`**
("there is facial geometric relation between gupta and pala facial anatomy … deleuzian intensive
differnces which came in pala through gupta … share high curvature eyes"), citing **3 grounds**
(`gnd_mrpi1jbv_3/4/5`), with 10 regions and **0 `text_blocks`** — i.e. the circulation was
genuinely unexercised before this probe.

## What was observed, in order

1. **Slash menu** — typing `/percept` in the Manuscript opened the BlockNote menu showing a
   **CHIASM › Percept** item: *"Mention a noticing — focus replays it on the image."*
2. **RefPicker** — invoking it opened "Which noticing?", listing the real percept's full
   expression with a **"3 grounds"** badge (matching the pre-state exactly).
3. **Chip inserted** — selecting it rendered the pill `✦ there is facial geometric relation
   between …` in the editor (`R2.0-01-chip-inserted.jpg`).
4. **Durable Mention attributes present** (DOM assertion):
   `data-percept-id="pctx_mrpi3rjk_0"`,
   `data-region-ids="gnd_mrpi1jbv_3,gnd_mrpi1jbv_4,gnd_mrpi1jbv_5"`,
   `data-mention-id="men_pctx_mrpi3rjk_0_block_…"` — the Mention's only durable form, correctly
   formed in local editor state.
5. **Recall fired on the image** — clicking the chip played the staged script
   (`R2.0-02-recall-recede-grounds.jpg`, `R2.0-03-recall-expression.jpg`):
   - the image **receded** behind a veil;
   - the cited **grounds highlighted over the Gupta and Pala heads** — semantically the right
     two faces for a percept about gupta↔pala facial geometry;
   - the **expression text** resolved over the image.
   This matches `buildRecallScript`'s recede → grounds → expression staging, now seen rendering
   rather than only unit-tested.
6. **Discarded via Cancel** — no save.

**No console errors** at any point.

## Pass / fail against the plan's criteria

| Criterion | Result |
|---|---|
| slash menu offers the percept | ✅ |
| chip inserts and renders with `data-*` attributes | ✅ |
| clicking the chip drives recall visibly on the image | ✅ |
| no console errors | ✅ |
| production post unchanged after discard | ✅ |

**Verdict: PASS.**

## Mutation check (level A held)

| Field | Before | After |
|---|---|---|
| `text_blocks` | 0 | **0** |
| `percepts` | 1 | 1 |
| `grounds` | 3 | 3 |
| `region_annotations` | 10 | 10 |
| curator identity hash | `030b23b2…` | **unchanged** |

Evidence: `evidence/R2.0-pre-state.json`, `evidence/R2.0-post-state.json`.

## Incidental observations (recorded, not fixed)

- The **RefPicker renders at the top-left of the viewport** rather than anchored near the cursor
  — cosmetic, did not impede the interaction. Reminiscent of the earlier slash-menu anchoring
  fix; worth a separate look, out of scope here.
- The **SAM chip shows active** on this worktree (SAM2 reports available), unlike the
  circulation worktree where refine 503'd. Not exercised by this probe.

## Scope held

No production entity, schema, route or collection created; no Passage/Inquiry/Discovery/
Embodiment; no Scheduler, Atlas or Codex; no circulation change; no corpus mutation; no
candidate graduated; nothing persisted (level A). The probe consumed **no VLM** — as predicted,
it is a pure frontend interaction over already-persisted percepts, grounds and regions.

## Next

R2.0's precondition is discharged. **R2.1–R2.6** (the six breadth rehearsals, families
R4/R5/R7/R8/R9/R12, under the amendment §8 controls) may be planned on approval — they are
**not** started here.
