# Frozen responses — local_qwen_vlm

Captured from run `20260822-final` on this machine. Every file here is a REAL response from
the local server, kept so `backend/tests/test_local_qwen_vlm_lab.py` can exercise the
audits and the schema without a model loaded. Nothing here was hand-written to make a
test pass; where a response is bad, the test asserts that the audit CATCHES it.

Files: experiment-1-observe, experiment-2-align-rich-tactile-philosophy, experiment-3-compare, experiment-4-reliability, experiment-5-canonical-prompts, census, gates
