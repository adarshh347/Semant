# Product Implications — {{RUN_ID}}

**Sandbox:** {{SANDBOX_ID}} · **Axis:** {{AXIS}} · **RESEARCH-ONLY**

> **This document authorizes nothing.** It records pressure, not proposals. Every item is
> **[SPEC]** until an explicit build gate says otherwise, and **no single sandbox can create a
> production entity** — see `foundry/GRADUATION-LADDER.md`.

---

## 1. Pressure observed

_What the finding pushes against in the product as it exists today. Cite the real file or surface —
a pressure named against an imagined architecture is not a pressure._

## 2. Nearest existing construct

_Ground · Percept · relation · Mention · VisionRun · an existing UI surface · none._
_The default answer is "an existing construct already covers this". Argue for that first._

## 3. What could be expressed with NO new entity

_The smallest honest move. Usually a behaviour, a count, a notification, or a field that already has
a home. This section should be the longest one here._

## 4. What would require a new entity — and why that is not yet justified

_State the new-entity temptation explicitly, then the reason it is premature: n too small, one model,
one seed pair, a design fork the program has deliberately not chosen._

## 5. Deliberately NOT concluded

_The inferences a reader will be tempted to draw and which this run does not license._

## 6. Cost and risk if built prematurely

_Interface taxonomic overload · premature ontology · geometry corruption · context flooding · hidden
behavioural profiling · false precision. Name which apply._

## 7. Reversibility

_If some version of this were ever prototyped: how it is turned off, what data it would strand, and
what the rollback is. A pressure with no rollback story is not ready to be discussed as a build._

## 8. Graduation status

**Current:** SPARK. **Required to advance:** see `graduation_rule` in the sandbox manifest —
repeated pattern → candidate card → build spec → prototype → product implementation, with a distinct
gate at each step.
