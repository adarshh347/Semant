# Foundry Horizon Brief — FS-001-creative-circulation

**Sandbox:** FS-001-creative-circulation · **Axis:** creative-circulation · **RESEARCH-ONLY**

> **Fragment.** Folds into `foundry/HB-FOUNDRY-B1-horizon-brief.md`. Sparks stay SPARK.

---

## 1. Header

FS-001 · creative-circulation · bounded task: score every percept in the frozen runs · **0 live calls
of 0 budgeted** · 0 production reads · no commits.

## 2. What axis moved

**Creative circulation moved from "unmeasured" to "unmeasurable on present evidence" — which is
movement, and not the kind hoped for.** Across the 11 posts the program holds frozen state for,
**0 carry writing**, including the 2 that carry percepts. The loop's second leg (percept → passage)
has zero instances. `Mention` is complete, 17-test-covered, and has never held a Mention.

**The axis did not move where the manifest predicted.** It was scoped to measure circulation yield;
it discovered the population has no positive class, so **the rubric failed its own `discrimination`
test** and is not yet a working instrument.

## 3. What product pressure appeared

**On process, not code.** The Candidate Foundry requires a circulation test before graduation; there
is nothing downstream to test against. Every R2 spark is blocked at that gate for a reason none of
them names. *(Speculative beyond this: nothing.)*

## 4. What repeated pattern emerged

**None — and that is reported honestly.** n = 3 percepts, 0 positive instances. The one *shape* worth
noting recurs from spark-07: percepts lack **scope**, and now appear to lack **time**. Two absences
found by two runs with empty positive classes is a reason to look for a cheaper common explanation,
not to add two fields.

## 5. What should be studied

1. **UQ1 — does any post in the wider corpus carry a passage citing a percept?** One read-only count
   kills or confirms the lane's central spark. **This sandbox cannot ask it** (critique §5). *Read:*
   score §2, critique §1.
2. **UQ2 — is the R1 rendered probe still blocked?** It died on exit-144 in R1 and was never
   rescheduled; R2 ran eleven model probes instead. **Servers stayed alive this session.** It is the
   cheapest unblocking action on this axis and has been waiting since R1. *Read:*
   `R1-existing-circulation-probe.md`.

## 6. What should NOT be built yet

- Any percept revision/version field, or a `PerceptRevision` entity.
- Any recall write-back path.
- A cross-post relation entity — named forbidden in the manifest precisely because it is attractive.
- Home surfacing percepts (evidence for it is second-hand in this run).

**Reason, one line:** zero positive instances is not a small sample, it is an empty class.

## 7. Method corrections

**The rubric was mis-designed and the run proved it** — `discrimination` scored `names-everything`.
Recorded as a method finding, not a table row. Also: `survival_of_detachment` cannot be scored from
A2R without double-counting the same fix under a second axis. **No earlier conclusion falls;** spark-03
loses one prop (its [SPEC] location assumes a downstream reader that does not exist).

## 8. Model / runtime constraints

None encountered. 0 calls, 0 DB reads, no adapter resolved. The binding constraint is
**infrastructural, not budgetary**: the axis is blocked by an unrun rendered probe.

## 9. Next execution expectation

**Next:** UQ1's read-only count, then the R1 rendered probe. **Precondition:** both are outside this
sandbox's declared inputs and need the orchestrator's instruction. **Stop condition:** the count
returns; the probe reaches "chip inserted, recall observed, discarded unsaved".

**Must NOT happen next:** any schema thinking about recurrence; any repair of detached grounds; any
production write.

## 10. Artifacts

`score.md` · `critique.md` · `sparks.md` (3 sparks) · `product-implications.md` · `seeds.json` (5/5
present) · `foundry-run.json` · this fragment.
**Production data: not mutated. No code changed. Live model calls: 0.**
