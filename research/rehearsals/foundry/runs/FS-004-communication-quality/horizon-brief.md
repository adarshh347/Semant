# Foundry Horizon Brief — FS-004-communication-quality

**Sandbox:** FS-004-communication-quality · **Axis:** communication-quality · **RESEARCH-ONLY**

> **Fragment.** Folds into `foundry/HB-FOUNDRY-B1-horizon-brief.md`. Sparks stay SPARK.

---

## 1. Header

FS-004 · communication-quality · bounded task: score every recorded utterance · **0 live calls of 0
budgeted** · manifest created this cycle · no commits.

## 2. What axis moved

**From "unexamined" to "examined and found to be a transmission problem, not an honesty problem."**
`visionActivity.js` discriminates *absent* from *unreadable* and carries a **tested** ban on causal
wording — then its rendered closed summary folds `partial`, `timed_out` and `stale` into `"N
recorded"`. The honesty is implemented one level below where it is spoken.

**Movement is modest and the run says so:** n = 4 utterances, only **2 user-facing**, **0 observed in
a browser by this run**.

## 3. What product pressure appeared

**The only lane in Batch 1 whose pressure reaches a shipped surface.** Precisely:
`differential/VisionActivityRail.jsx`'s closed line renders a count where the module beneath holds a
distinction — and the drawer is collapsed by default, below the counts footer.

**This is already scoped as HW-S1 experiment E5**, with location, home module, test file and kill
condition written. *Belongs in an existing surface; wants no new one.*

## 4. What repeated pattern emerged

**One, weak but genuine: every user-facing utterance closes rather than invites.** Both shipped
sentences inform or terminate; neither offers a next act. **This converged independently with
FS-001**, which found the second turn has never occurred — an honest announcement that ends the
encounter cannot produce circulation.

**Caveat, stated plainly:** one analyst wrote all three lanes in one order, so cross-lane convergence
**cannot be claimed as independent** (critique §3). The only unforced convergence is UQ3, which both
FS-001 and FS-004 reached separately: *does Chiasm render A2R's evidence note in its own pane?*

## 5. What should be studied

1. **Whether to run HW-S1's E5.** It already exists, fully scoped and reversible. **The product
   action here is a decision, not a design.** *Read:* HW-S1 §1.2 and E5; product-implications §3.
2. **UQ2 — can the closed summary carry the non-nominal distinction without opening the drawer,** or
   are those states unknown until load? This decides whether the silence is a *choice* or a
   *consequence*, and it is the difference between a finding and an artifact. *Read:* critique §5.

## 6. What should NOT be built yet

- **An overclaim detector.** Deeply attractive after spark-10 and the clearest premature-ontology
  risk in the batch: **spark-10's deflationary reading is still live and `012-repeat-stability` may
  invalidate the whole line.**
- A tone system, voice guide, or copy convention — two user-facing samples license no system.
- A stored communication-quality or confidence score — false precision on n = 2.
- **Opening the rail by default** — HW-S1 explicitly considered and declined it.
- Any refusal copy: **no refusal has ever occurred** in eleven arms; drafting one invents the evidence.

## 7. Method corrections

**Three, all self-inflicted and all recorded.** (1) The score's headline — *"the denominator is the
mechanism"* — **overreached and is downgraded**: B may have no natural denominator, and the
defensible claim is only that it discards a distinction it already computes. (2) Model output (D) was
scored on the same table as UI copy (A, B) after the score itself named that as `analogy_overreach`.
(3) *"Exactly one render-verified honest sentence"* is unverified as a count — it is one **in the
research record**; the product's copy was never enumerated.

**No earlier conclusion falls.** A2R, HW-S1 and spark-10 are restated, none contradicted; this run
adds no evidence that moves any of them.

## 8. Model / runtime constraints

None. 0 calls. Note for the next executor: **this lane's subject matter lies partly outside its own
declared inputs** — enumerating the product's real copy needs a sandbox whose `allowed_inputs` permit
it, and widening them is a manifest decision, not a runtime one.

## 9. Next execution expectation

**Next:** an orchestrator decision on E5. **Precondition:** HB-010's standing constraint — **no UI
string changes without an explicit implement instruction** — remains in force and nothing here
relaxes it. **Stop condition:** the decision is recorded; if yes, E5 ships alone with its own kill
condition.

**Must NOT happen next:** any UI copy written by a sandbox; any detector built for spark-10 before
012 runs.

## 10. Artifacts

`FS-004-communication-quality.yaml` (manifest, new) · `score.md` · `critique.md` · `sparks.md` (3
sparks) · `product-implications.md` · `seeds.json` (4/4 present) · `foundry-run.json` · this fragment.
**Production data: not mutated. No code changed. No UI copy written. Live model calls: 0.**
