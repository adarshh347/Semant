# R1 → R2 recommendation

## R1 status

**Success, with one honestly-blocked probe.** The research substrate can represent a rich
reconstructed run honestly (Passage 001 = run #0, 14 events, all reconstructed, null telemetry
preserved); future instrumented observations can be captured and replayed without confusing live
model output with determinism (capture=2/replay=0 proven); no production ontology or curator data
changed (25 tests + safety asserts). The one unmet R1 success criterion — *rendered*-verifying the
existing expression-Percept → chip → recall loop — is blocked by infrastructure and reported as such,
not faked.

## Smallest safe R2

R2 is the **breadth batch** (six rehearsals, `R0-breadth-portfolio.md`), but it must open with a
**precondition**, not a rehearsal:

**R2.0 — the deferred rendered probe.** Before any new rehearsal, get a server to stay alive (a
session without the exit-144 killer, or a user-run local instance) and execute the level-A probe on
`/posts/695be6c9…`: edit mode → `/percept` insert `pctx_mrpi3rjk_0` → click → observe recall on the
image → **discard without saving** → confirm the production post's `text_blocks` stayed empty.
Capture screenshots. Outcome: the existing loop is *rendered-verified* or *reported broken*. Only
then design anything.

**R2.1–R2.6 — the six rehearsals** (families R4/R5/R7/R8/R9/R12), run through the R1 runner in
**instrumented** mode with the amendment §8 controls: A3 uses a curator-selected comparison (+
optional embedding neighbour + a negative comparison; embedding similarity must **not** define
gesture relevance); A6 requires curator-supplied cultural-difference metadata (the model may not
declare two objects unrelated without evidence); every run records `image_order` and
`source_condition`. At least two must stall/refuse. Fable conducts + meta-observes (separate passes);
Opus instruments the read-only adapters.

## What R2 must NOT build

- **No** production entity/schema (Passage, Inquiry, Discovery, Embodiment, Candidate, Field,
  context-memory), route, or Mongo collection.
- **No** Atlas, Codex, or cross-image structure; **no** Differential/Writer redesign.
- **No** agent-skill persistence (R6 only).
- **No** graduation of any candidate (foundry needs ≥3 fixtures + transfer/negative first, R3).
- **No** Groq repoint; **no** 118-post expansion; **no** corpus mutation; **no** PR merge.
- **No** live sensory adapter beyond what a specific rehearsal genuinely needs — and heavy models
  (SAM/DINOv2/VLM) only with explicit per-rehearsal approval + capture-then-freeze, never "to
  demonstrate availability."

## Sequencing after R2

`R2 (probe + 6 rehearsals)` → **stop, review scores/traces** → `R3 transfer/negative/sequence/
counter-reading trials on ≤4 candidates` → `R4 ≤2 reversible prototypes` → `R5 one circulation slice
(adapt the strongest of PASSAGE-CIRCULATION here)` → `R6 skills` → `R7 expansion`.

## One line

Approve **R2 = the deferred rendered probe first, then the six breadth rehearsals in instrumented
mode with the §8 controls** — building no entity, no surface, no Atlas/Codex — so repeated candidates,
not eloquence, choose the eventual build.
