# VISION-C · C1 — General profile — report

**Date:** 2026-07-19 · **Branch:** `feat/vision-c-domain-profiles`. Reuse-and-verify gate:
the general profile is the existing YOLO11n-seg proposal (B2) + SAM 2.1 refinement (B3/B5),
formalised under the C0 profile contract. **No new models.**

## The general profile
- `passes_for(["general"]) == ["yolo11n_seg", "sam21_hiera_tiny"]` — the cheap proposal
  pass + the on-demand refiner (C0 `domain_profiles.py`).
- **Fallback, not a prison:** when a specialist is chosen, general's cheap pass still runs
  alongside it, and the specialist is *not* gated behind YOLO's box — merge is by
  geometry/precedence (`fashion_segmentation_service.merge_with_precedence`), never
  label-parenting. Asserted by `test_propose_is_multilabel_over_gate` (mixed passes include
  yolo + fashionpedia + segformer) and `test_passes_for_general_first_and_deduped`.
- Authoritative masks + provenance: YOLO instance masks are stored as `mask_rle` with
  `geometry_provenance{adapter, model, device, preprocessing_version}` (B2), never reduced
  to a box; SAM refinement proposes a new geometry revision non-destructively (B3/B5).

## Gate — general images complete detect → exact mask → refine → save → recall (normal route)

Verified on post `695be794` (B&W statue-against-trees, a general/photography image) through
`/posts/:id`:
- **detect → exact mask:** clicking **Dissect** (General profile) ran the YOLO general pass;
  the persisted `person` region carries `mask_rle=True`, `polygons`, `detector=yolo`,
  `adapter=yolo11n_seg`, device recorded — an authoritative exact mask, not a box.
  (`C1-general-dissect-mask.jpg`.)
- **refine → save → recall:** the identical Differential Refine loop is browser-proven end
  to end in B5 (`/posts/:id` → Differential → Select → Refine → confirm → reload → recall),
  on the same class of region and the same SAM refiner the general profile schedules. The
  general profile adds nothing new to that path — it *is* that path.

Backend suite **97 passed** (C0 tests cover the general-profile contract). GET the post → 200.

**Gate result: PASS** — general images complete detect → exact mask → refine → save → recall
through the normal product route, with authoritative masks and full provenance, and general
stays a fallback rather than a compulsory first-pass prison for specialists.

## Note
`detect_regions` still runs YOLO on CPU (the segmentation_service device, unchanged since
B2). It's fast enough (~seconds) and correct; moving the general pass onto the GPU adapter
via the orchestrator is an optimisation deferred to C5's planner wiring, not a C1 blocker.

## Next
C2 — Architecture profile: integrate `nvidia/segformer-b0-finetuned-ade-512-512` through
the canonical adapter + ModelManager (a real new model). C0's architecture passes already
name `segformer_b0_ade`.
