# VISION-F · F7 — Expansion decision (not a silent backfill) — report

**Date:** 2026-07-20 · **Branch:** `feat/vision-f-recovery` · **No corpus-wide mutation run.**

## Representative-batch result (recap)

6 posts recovered/reviewed via the ledger + backup: dissected regions **15/42 → 41/42
mask-authoritative**; every Region id + label + curator decision preserved (curator-only hashes
unchanged); 4 detached grounds flagged; the 26 stale embeddings refreshed (no duplicates,
version-isolated); retrieval **identity same-label@5 55% → 68%**. Full details in F0–F6 reports.

## The remaining corpus — resolved plan

| set | posts | work | cost | review burden |
|-----|-------|------|------|---------------|
| **remaining dissected** | 3 | 1 polygon-derive + 3 retain_mask (no SAM) | ~seconds GPU, ~0 storage | ~1 region |
| **undissected (expansion)** | 118 (all have images) | dissect (YOLO+SegFormer+SAM) + embed (+ optional semantic read) | **~39 min GPU dissect + ~13 min embed, ~3.6 MB, ~$0.12 API** | **~551 new regions** |

Estimates: ~551 new regions (@~4.7/post), ~1,219 new vectors (E6 dry-run + F4 measured throughput).

- **Skip/failure list:** 0 posts lack images; no capability is down (F2 matrix all green), so no
  planned skips. The 1 remaining box-legacy region (in `aef5f7`) upgrades via polygon-derive.
- **Rollback readiness:** every mutated post is restorable per-post from the F1 backup + ledger;
  the expansion would extend the same backup/ledger before touching new rows.
- **Licensing / deployment constraints:** YOLO11-seg **AGPL-3.0**; SegFormer-B0 ADE20K
  **NVIDIA research (non-commercial — flag before any commercial deploy)**; SAM2.1 Apache-2.0;
  DINOv2 Apache-2.0; FashionCLIP per model card; semantic reads via OpenRouter gpt-4o-mini (API
  cost + external dependency). The non-commercial SegFormer license is the binding constraint for
  a commercial deployment of the architecture profile.

## Recommendation

1. **Finish the 3 remaining dissected posts** (trivial, ~seconds, low-risk) to complete the
   *recovery* mandate to 42/42 masks — recommended, on your go-ahead.
2. **Do NOT run the 118-post expansion as a blind corpus-wide backfill.** The GPU/storage cost is
   small (~50 min, ~3.6 MB), but the **binding constraint is the ~551 regions of curator review**
   the F3 quality gate requires — dissecting unreviewed posts risks re-introducing exactly the
   box/sliver/mislabel issues F just repaired. Expansion should run in **bounded, curator-reviewed
   batches through the same ledger**, stopping on threshold breaches, not in one automatic sweep.
3. **No new models.** OpenCLIP / score-fusion stay deferred (F6): the corpus did not grow and the
   retrieval gain came from geometry, not a new space.

## Authorization

Per the increment contract, the remaining corpus-wide mutation is **not executed** without explicit
authorization after this report. Awaiting your decision on (1) finishing the 3 remaining dissected
posts and (2) whether/how to stage the 118-post expansion.

## Post-F horizon (recommended next)
- **G — bounded, reviewed corpus dissection:** stage the 118 undissected posts in small
  curator-reviewed batches (the review burden, not compute, is the gate).
- Revisit **OpenCLIP / text↔evidence** and **score fusion** only once a genuinely multi-domain
  corpus exists to evaluate them against.
- Address the **same-image retrieval bias** (corpus density) and consider a per-domain
  material/colour de-biasing step for rank-1.
