# VISION-F · F0 — Post-merge smoke test + corpus audit — report

**Date:** 2026-07-19 · **Branch:** `feat/vision-f-recovery` · **Writes: ZERO** (enforced).

## Method

`scripts/vision_f0_audit.py` runs strictly read-only: a **write-guard** monkeypatches every
mutating Mongo call (`update_*`, `insert_*`, `delete_*`, `replace_*`, `find_one_and_*`,
`bulk_write`) on both `post_collection` and `region_embeddings_collection` to raise. The script
completed with no guard trip — **no write was attempted**. Output: an immutable manifest
(`F0-audit/manifest.json`, sha256 `7c209e64…`, 127 post entries) + this summary.

## Smoke test (service-level, real curator-route components)

| capability | state |
|-----------|-------|
| image fetch + decode | ok (1024×672) |
| YOLO / SegFormer / SAM2 adapters | **available** |
| DINOv2 adapter | **available** |
| semantic VLM provider (OpenRouter gpt-4o-mini) | **ready** |
| Fashionpedia | deferred (serverless — expected) |
| retrieval spaces | visual identity/context/whole + fashion **available**; text_image (OpenCLIP) deferred |
| retrieval search (stored vector) | ready (3 hits) |

The HTTP server was not used for F0 (it is not needed for a read-only audit and was unstable in
this session state; browser routes are exercised live at F3/F5). All curator-route components are
individually confirmed runnable.

## Corpus audit (127 posts)

- **Images:** 127/127 have a `photo_url`; reachability recorded per post in the manifest.
- **Dissection:** **9 dissected**, **118 undissected** (never processed — expansion candidates,
  not repair targets).
- **Regions (42 total):**
  - **mask-authoritative: 15**
  - **polygon-only: 9** — legacy, no `mask_rle`
  - **box-only: 18** — legacy, weakest evidence
  - none: 0
  → **27 regions (9 + 18) lack a canonical mask** — the F3 geometry-recovery target.
- **Coordinate corruption fingerprints: 0** — no slivers, no crush, no out-of-bounds coords.
- **Detached: 4 grounds**, 0 percepts. 24 grounds, 5 percepts, 0 writing-mentions detected.
- **Semantics:** 1 post carries assertions (the Pietà); **2 curator-curated** (accepted/edited).
- **Curator signals to preserve:** 5 creator-authored regions, 2 curated assertions, 24 grounds,
  5 percepts. (0 region notes / prioritised / weighted currently.)
- **External refs to preserve:** `photo_public_id`, `source_url`, region `embedding_id` pointers.

## Embeddings inventory

97 vectors — `dinov2 identity 42 / context 42 / whole 9`, `fashion 4` — all in clean versioned
spaces (no legacy-unspaced rows), **0 stale, 0 orphaned**.

## Re-evaluation of the "corrupted-post" count (F0.4)

**The old assumption of many coordinate-corrupted posts does not hold against live data.** The
live truth is: **0 coordinate-corrupt regions**; the real recovery surface is **27 legacy
non-mask regions across the 9 dissected posts** (9 polygon-only + 18 box-only) plus **4 detached
grounds**. The 118 undissected posts are an *expansion* question (F7), not repair. This reframes
F3 from "fix corruption" to "raise legacy geometry to canonical masks where defensible, honestly
retain box-only where not."

**Gate result: PASS** — immutable audit report + deterministic machine-readable manifest
produced; zero database writes (guard-enforced).

## Next
F1 — scoped backup of the manifest posts + sidecar rows, migration ledger, and a restore drill
on a disposable copy — before any mutation.
