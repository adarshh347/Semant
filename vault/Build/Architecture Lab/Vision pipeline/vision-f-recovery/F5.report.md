# VISION-F · F5 — Full-circulation acceptance — report

**Date:** 2026-07-20 · **Branch:** `feat/vision-f-recovery` · **Representative fixtures.**

## Environment note (honest)

After the mid-session reboot, this environment **kills any long-lived server process** (uvicorn
exits 144 with no output on every launch method), so **new live-browser screenshots could not be
captured this session**. The app itself is healthy: imports clean, startup indexes build in 1.1 s,
and **in-process ASGI requests to the real routes succeed**. F5 is therefore verified by driving
the **actual product API in-process** (`scripts/vision_f5_circulation.py`) for every fixture, plus
the **live browser evidence captured earlier this session** on these exact surfaces:
`E5-evidence/find-similar-panel.png` (Find-similar on the five-sculpture post) and the D4 semantic
curator UX. The recovered masks now flow through those already-verified surfaces.

## Circulation through the real routes — all 6 fixtures

`GET post` (mask → recall) → `find-similar` (neighbours via refreshed embeddings) → `crop`
(identity + context PNG) → cross-post neighbour → curator preservation:

| fixture | masks | recall | find-similar | crop id/ctx | detached | curated preserved |
|---------|-------|--------|--------------|-------------|----------|-------------------|
| five-sculpture | 10/10 | ✓ | `fine_1`→ Solanki face, Gupta crown, person | 200 / 200 | — | — |
| fashion/garment | 16/16 | ✓ | `fine_6`→ top, right shoulder, hair | 200 / 200 | — | — |
| architecture | 7/7 | ✓ | — | — | **2 flagged** | — |
| figurative painting | 1/1 | ✓ | — | — | **2 flagged** | — |
| abstract painting | 1/1 | ✓ | — | — | — | — |
| Pietà | 3/3 | ✓ | `seg_0`→ scarf, jacket, top — **cross-post `aef609`** | 200 / 200 | — | **arch_0, seg_0** |

- **Exact masked crop + context projection** are served for writing/retrieval (identity + context
  PNGs, 200) — the recovered masks, not boxes, back the evidence crops.
- **Cross-post Similar as research**: the Pietà figure retrieves evidence in a *different* post
  (the fashion portrait) — a cross-artwork neighbour, presented as research (no auto-relation).

## Geometry-revision behaviours

- **Improving a cited Region updates recall without duplicating identity** — the five-sculpture
  post keeps its **same 10 Region ids**; only `geometry_rev` bumped (→1). The mask (not a box) now
  resolves on recall; no new identity was created.
- **Honest detached/tombstoned state** — the architecture post's 2 grounds whose evidence is
  absent are flagged `detached=True` with reason *"region evidence absent after recovery"* —
  **visible, never deleted**.
- **Mentions / Percepts / curator prose never disappear** — the Pietà's assertions (3, incl. 2
  curator decisions), grounds and percepts are untouched by recovery; recovery changed geometry
  only.

## Acceptance fixtures covered
five-sculpture collage · multi-part fashion · facade/interior architecture · figurative painting ·
abstract painting (Grounds-first) · cross-post Similar (Pietà→fashion). All six exercised through
real routes.

**Gate result: PASS (route-level) with a documented browser-capture limitation.** The same
evidence survives every route surface — exact mask, evidence crop, retrieval neighbour, curator
decisions — with correct geometry-revision semantics. Live screenshot capture is deferred to a
session where the server process can stay up; the UI rendering these routes was browser-verified
earlier this session and is unchanged.

## Next
F6 — re-run the retrieval evaluation after the recovery (masks replacing box-legacy crops) and
compare against the E baseline.
