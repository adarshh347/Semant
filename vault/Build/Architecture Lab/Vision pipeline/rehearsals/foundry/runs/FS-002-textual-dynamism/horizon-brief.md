# Foundry Horizon Brief — FS-002-textual-dynamism

**Sandbox:** FS-002-textual-dynamism · **Axis:** textual-dynamism · **RESEARCH-ONLY**

> **Fragment.** Folds into `foundry/HB-FOUNDRY-B1-horizon-brief.md`. Sparks stay SPARK.

---

## 1. Header

FS-002 · textual-dynamism · bounded task: identify when a textual slice changed what was visible ·
**0 live calls of 0 budgeted** · no books ingested, no long quotation · no commits.

## 2. What axis moved

**The axis split in two, and only one half exists.** The program has two clean demonstrations that
**one word in a question changes what the model reports seeing** (spark-06: *address* → a face on a
facade; spark-09: *motif* → shared-purpose talk), and **zero instrumented runs in which a source text
was present at all**. "Source pressure" and "prompt vocabulary" have been spoken of as one thing.

Answer to the lane's question: **prompt text is decisively a live force; source text is untested.**

## 3. What product pressure appeared

**Almost none — and saying so is the honest result.** All four sparks land on research artifacts
(`rehearsal-manifest.schema.json`, the R2 batch plan) that define no production entity.

One indirect thread does reach the product: if the question *is* the instrument, then the fixed
prompts inside Aletheia `/read` and the stubbed *Draft from the image* / *Continue writing* actions
are load-bearing, and changing their wording is not copy-editing. *(Speculative.)*

## 4. What repeated pattern emerged

**A real one, across two vocabularies:** *address* (spark-06) and *motif* (spark-09) both show the
question supplying what looked like a property of the image. Two relational vocabularies now behave
the same way. **That is a pattern to name, not a disposition to assert** — and it is inherited from
runs 007–009, not produced here.

## 5. What should be studied

1. **UQ2 — does the program still want an `absent`/`misleading` source arm now that A6 is retired?**
   Without one, source pressure stays permanently untested and the doctrine's ventriloquism remedy
   stays theoretical. **If the answer is no, textual dynamism should be closed as an axis rather than
   left implicitly open.** *Read:* score §3 R4b, sparks spark-FS002-03.
2. **UQ1 — was `source_condition: present` on eleven runs a mistake or a shorthand?** The field's
   *meaning* is settled (`R1-schema-decisions.md`); the *authors' intent* is not, and guessing intent
   is a declared stop. **Only the orchestrator can answer.** *Read:* score §2 E1/E2b.

## 6. What should NOT be built yet

- A `SourceSlice` / `Pressure` entity — zero instrumented instances; the one designed test was retired.
- Ingestion of Warburg / Casey / Didi-Huberman. Named as gaps, **not** proposed as acquisitions —
  HW-C5 is explicit that **images, not texts, are the bottleneck**.
- Any retro-fix of `source_condition` on the eleven runs. **Runs are frozen evidence** (HW-C6:
  *"Do not retro-fill the runs"*).
- Reopening A6. The ceiling argument that retired it stands.
- Centrally managed / versioned prompt copy in the product.

## 7. Method corrections

**Three, and two matter.** (1) The critique's strongest objection — that `source_condition` might not
mean "a source was supplied" — **was raised, tested against `R1-schema-decisions.md`, and refuted**;
the finding survived its own attack. (2) The score's tone about source absence was **unearned**: the
plan shows A3 dropped Merleau-Ponty *deliberately* to avoid ventriloquism, so the gap is partly a
guard, not an oversight. (3) **FS-002's declared human-prose control was frozen as a seed and never
scored** — a real method failure that weakens the `discrimination` row below its recorded value.

**No earlier conclusion falls.** spark-06 and spark-09 are untouched and must not be cited as
replicated by this run.

## 8. Model / runtime constraints

None. 0 calls. The only operational note: `paraphrase` + `locator` discipline held trivially, because
nothing was under pressure to cite anything.

## 9. Next execution expectation

**Next:** an orchestrator decision on UQ2 — keep the axis open with a source arm, or close it.
**Precondition:** none; it is a judgement, not a run. **Stop condition:** the decision is recorded.

**Must NOT happen next:** any manifest editing of frozen runs; any text acquisition; any claim that
the program was ventriloquizing (it was not — refuted).

## 10. Artifacts

`score.md` · `critique.md` · `sparks.md` (4 sparks) · `product-implications.md` · `seeds.json` (4/4
present) · `foundry-run.json` · this fragment.
**Production data: not mutated. No code changed. No run manifest edited. Live model calls: 0.**
