# Foundry Sparks — FS-004-communication-quality

**Sandbox:** FS-004-communication-quality · **Axis:** communication-quality · **RESEARCH-ONLY**

> **A sandbox may emit only SPARKs.** Nothing graduates in its originating run.

---

## Spark register

### spark-FS004-01 — the surface discards a distinction the module already computes

- **Origin thread:** score §3 R2, **as corrected by critique §4** (the original "denominator is the
  mechanism" framing is downgraded).
- **Enablement claim:** `visionActivity.js` discriminates *absent* from *unreadable* and has a
  **tested** prohibition on causal wording. Its rendered closed summary folds `partial`, `timed_out`
  and `stale` into `"N recorded"`. The honesty is implemented; it is discarded at the rendering
  boundary. **Semant's communication gap is not a lack of honesty but a lack of transmission.**
- **Evidence:** `HW-S1-frontend-opportunity-scout.md` §1.2 (internals + tested causal-wording ban)
  and E5 (the closed summary folds non-nominal states).
- **Nearest existing Semant construct:** `visionActivity.js`'s own derivation surface — HW-S1 notes
  the tone vocabulary already exists there. **No new entity; not even new data.**
- **Strength:** SPARK, n = 1 surface, **evidence entirely second-hand** (a scout's reading, not
  observed here). Restates HW-S1 E5 with a reason attached.
- **What would strengthen it:** a second surface holding a distinction it does not render.
- **What would kill it:** showing the states are unknown until the drawer's payload loads — then the
  silence is a consequence, not a choice.
- **[SPEC] possible location:** HW-S1's E5 already names it. **Nothing new proposed here.**

### spark-FS004-02 — every user-facing utterance closes; none invites a return

- **Origin thread:** score §3 R5.
- **Enablement claim:** both shipped utterances *inform* or *close*. A tells you the evidence is gone
  and offers no next act; B hides its detail behind a collapsed drawer. **An honest announcement that
  terminates the encounter cannot produce circulation** — which is precisely the second turn FS-001
  found has never occurred. Trust and recurrence may be the same axis seen twice.
- **Evidence:** A (`A2R`), B (`HW-S1` §1.2); FS-001 score §2 E1/E2 for the zero-second-turn fact.
- **Nearest existing Semant construct:** none — this is a property of copy, not of the data model.
- **Strength:** SPARK, n = 2 utterances, and **the dimension carrying it is inferred from text with
  no reader present** (critique §3). The weakest evidence in the batch supporting the most
  interesting claim.
- **What would strengthen it:** one observation of a curator meeting utterance A.
- **What would kill it:** a user reading A and immediately re-pointing the evidence — that would make
  it an invitation despite its wording.
- **[SPEC] possible location:** deliberately not proposed. Naming a next act is UI copy, which this
  sandbox forbids.

### spark-FS004-03 — the measured failure has no counterpart guard anywhere in the product

- **Origin thread:** score §3 R4.
- **Enablement claim:** spark-10 established that as content thins, force rises (*"powerful and
  unifying"* on a pair two lanes called weak). Every surface that will render model prose — Aletheia
  `/read`, the stubbed *Draft from the image* / *Continue writing* actions — inherits that behaviour.
  **Nothing in the four utterances, or anywhere the record describes, would notice it.** "Cite your
  evidence" is satisfied by D; the grain of the claim is not constrained.
- **Evidence:** `011/score.md` §1; HB-010 §7 (*"amplifies force while diluting content"*);
  `Findings/HW-C10-abstraction-as-immunisation.md` via the register.
- **Nearest existing Semant construct:** **none, and none is proposed.** spark-10's own register entry
  says the same: *"a fact about a model's rhetoric, not about Semant's ontology."*
- **Strength:** SPARK. Inherits spark-10's strength (n = 2 calls, 2 pairs) and adds only the
  observation that no guard exists. **The deflationary reading of spark-10 is still live** and is
  carried here unchanged: independently generated passages may simply fail to interact.
- **What would strengthen it:** run `012-repeat-stability`, which is already the program's
  highest-priority queued call and can invalidate the whole line.
- **What would kill it:** 012 showing spark-10 does not replicate.
- **[SPEC] possible location:** none. Do not build a confidence detector.

---

## Withheld

- **A rewritten rail summary.** I can write it; the manifest forbids it and the stop condition names
  it. Withheld — and score §8 records this was the hardest withholding in the run.
- **A tone system, voice guide, or copy convention.** Two user-facing samples cannot license a system.
- **"Semant communicates untrustworthily."** False. A is genuinely good; the rail's internals are
  better than most of the product.
- **"Open the rail by default."** HW-S1 considered and declined it. Repeating a rejected idea as a
  finding would launder it.
- **A refusal sentence for the model.** Eleven arms, zero refusals — the program has no sentence for
  refusal because it has never seen one. Drafting one would be inventing the evidence.

## Unresolved questions

- **UQ1** — How many honest utterances does the product actually contain? This run enumerated the
  ones the *research record* discusses, not the ones that *exist* (critique §4, correction 3). A
  bounded read-only copy inventory would settle it; **outside this sandbox's inputs.**
- **UQ2** — Can B carry the `partial`/`timed_out`/`stale` distinction without opening the drawer, or
  are those states unknown until load? This decides whether spark-FS004-01 is a choice or a
  consequence.
- **UQ3** — Does Chiasm render A2R's note in its own pane? A2R explicitly declines to assume it.
  **Same question as FS-001 UQ3** — two lanes independently reached it, which is the batch's only
  unforced convergence.
- **UQ4** — What should Semant say when it refuses? Currently unanswerable: no refusal has occurred
  in eleven arms, and HB-010 §10(4) notes it is "starting to look like a property rather than an
  accident."
