# P1.1 — Test Evidence

Environment: project `venv`, no live Mongo / network / models. Same house style as P1 (sync
tests via `asyncio.run` + injected fake collections).

## Counts

- **P1 suite** `test_circulation_spine_p1.py`: **40 passed** (28 P1 + **12 new P1.1**).
- **Targeted regression** (geometry / segmenter / merge / domain / orchestrator / semantic /
  recovery): **122 passed**.
- **Full backend suite** `backend/tests/`: **265 passed** (was 253; +12). Zero failures; no
  unrelated pre-existing failures.

```
$ python -m pytest backend/tests/test_circulation_spine_p1.py -q   → 40 passed
$ python -m pytest <9 regression suites> -q                        → 122 passed
$ python -m pytest backend/tests/ -q                               → 265 passed
```

## New P1.1 tests → correction map

| Correction | Tests |
|---|---|
| 1 · HTTP retains full event contract | `test_response_model_retains_run_id_and_contract_version` |
| 2 · cross-run ownership | `test_append_event_sets_absent_run_id` · `…_accepts_matching_run_id` · `…_rejects_conflicting_run_id` |
| 3 · index matches query | `test_ensure_indexes_declares_only_the_supported_compound` |
| 4 · zero-match persist not a false success | `test_zero_match_persistence_records_failed_not_success` |
| 5 · bounded payloads | `test_bounded_payload_rejects_binary_and_unsupported` · `…_rejects_excess_nesting_cardinality_and_length` · `…_applies_to_run_fields` · `test_normal_route_payloads_stay_valid` |
| 6 · error bounding | `test_error_text_is_truncated_not_dropped` |
| write-behind preserved for new guards | `test_recorder_swallows_bounded_and_mismatch_errors` |

## Required proofs (re-verified)

| Proof | Evidence |
|---|---|
| failing telemetry does not alter route output | `test_route_output_equivalent_with_working_and_failing_telemetry` (carried from P1) + `test_recorder_swallows_bounded_and_mismatch_errors` |
| primary exceptions remain primary | `test_route_reraises_primary_exception_and_marks_run_failed` |
| geometry / curator state unchanged | `test_route_output_equivalent_…` + 122-test regression green |
| response-model projection retains the complete event contract | `test_response_model_retains_run_id_and_contract_version` |
| zero-match persistence not reported as success | `test_zero_match_persistence_records_failed_not_success` |
| cross-run event mismatch cannot persist | `test_append_event_rejects_conflicting_run_id` |
| oversized / binary telemetry cannot enter Mongo | `test_bounded_payload_*` + `test_recorder_swallows_bounded_and_mismatch_errors` |

## Not claimed

Nothing `rendered-verified`. No live-DB / live-model run (exit-144 blocker). All P1.1 claims
are `logic-tested` with in-memory fakes. See `P1-P2-recommendation.md` — P2.0 live
verification remains the required first step before any surface.
