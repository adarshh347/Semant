# CIRCULATION-SPINE-001 · P2.0 — Live Backend Verification

Closes the one gate P1/P1.1 left open: **`rendered-verified`-class live proof**. Every P1
claim was previously `logic-tested` with in-memory fakes; this run exercises the **real**
`detect_regions` route and the **real** read handlers against the **real** `visualDictionaryDB`.

**Result: 26/26 checks PASS. Database restored to baseline (posts 127 → 127, vision_runs
0 → 0).** Branch `feat/circulation-spine-001-p1` @ P1.1 `1024725` (+ doc relocation `7a13958`).

Tags: `live-verified` (new, this gate) · `logic-tested` · `blocked`. No frontend, no sibling
routes, no merge, no handoff regeneration.

## Method — how the exit-144 blocker was sidestepped

The blocker only ever killed **long-lived** servers (`uvicorn`, `npm run dev`); short-lived
in-process scripts work. So verification ran the actual async route/handler functions
in-process against real Motor collections — a live backend exercise without a standing HTTP
server. Isolated circulation worktree; real DB; **disposable real post** seeded with a known
creator region; everything cleaned up in a `finally` block.

Script: `evidence/` (re-runnable). Raw artifacts: `p2.0-verification-evidence.json` (26
checks + counts), `p2.0-retrieved-run.json` (the live `vision_runs` document).

## Subject

A disposable post inserted into real Mongo (`_id 6a5ec746…e26d`), `photo_url` = a real
Cloudinary image from an existing post (read-only reuse), seeded with one **creator** region
carrying `mask_rle {size:[64,64], counts:"P20_SEED_RLE"}`, `weight 7`, `user_note`. Deleted
at the end; the DB never retained it.

## The live run (real trace, `coarse_only=True`)

`run_id 6a5ec746…e26e` · `status succeeded` · `operation dissect` · `initiator api` ·
`source "yolo"` · `terminal_reason ok` · `telemetry_degraded false` ·
`result_summary {anchor_count:1, fine_count:0, creator_preserved:1, region_count:2}`.

| stage | status | latency_ms | adapter | detail |
|---|---|---|---|---|
| dissect.receive | succeeded | — | — | coarse_only=true |
| dissect.fetch_image | succeeded | **250.3** | — | |
| dissect.route_domain | succeeded | — | — | is_fashion=false, chosen=[] |
| dissect.segment.general | succeeded | **930.6** | yolo11_seg | count=1 |
| dissect.merge_anchors | succeeded | — | — | anchor_count=1, source=yolo |
| dissect.merge_curator_state | succeeded | — | — | creator_preserved=1, kept_auto=1 |
| dissect.canonicalize_geometry | succeeded | — | — | region_count=2 |
| dissect.persist_regions | succeeded | — | — | region_count=2, **matched_count=1** |
| dissect.complete | succeeded | — | — | anchor_count=1, fine_count=0 |

Latencies are **real `perf_counter` deltas** from live model/network calls; unmeasured stages
correctly carry `null` (never fabricated).

## Requirement-by-requirement

### V1 · Persistent run creation `live-verified`
Route returned a `run_id`; a `vision_runs` document was found in real Mongo by that id
(`status succeeded`, 9 events), scoped `post_id` = the disposable post, `operation = dissect`,
terminal status. (5/5)

### V2 · Both retrieval routes `live-verified`
`GET /vision-runs/{run_id}` (`get_vision_run`) returned the run with matching id/post and all
9 events; `GET /vision-runs/latest` (`get_latest_vision_run`) returned the same run; a request
scoped to a **different** post raised **HTTP 404** (ownership enforced). (4/4)

### V3 · Actual stage correspondence `live-verified`
The 9 persisted stages correspond to what actually executed: receive/fetch/route present;
`decompose_fine` **absent** (coarse_only); `persist_regions = succeeded` (real post → matched);
segment stages match the `source` string (`is_fashion=false` → only `segment.general`, no
fashion/arch/fallback). Nothing emitted from a plan. (5/5)

### V4 · Authoritative geometry equivalence `live-verified`
Persisted `region_annotations` **==** the route's returned `regions` (byte-equal, 2 regions);
the seeded creator region survived intact with `mask_rle` unchanged and curator fields
(`weight 7`, `user_note`, `prioritised`) preserved; the auto region carries **real YOLO
`mask_rle`** authoritative geometry; and **no event payload contains any geometry key** —
masks stay in `region_annotations`, never in telemetry. (6/6)

### V5 · Write-behind under an intentionally failing telemetry store `live-verified`
The telemetry collection was swapped for a store whose `insert_one` raises, while the post
write stayed real. The route still returned regions with **`run_id: null`**; the real
`region_annotations` write **still happened** (creator preserved); and **no run document was
created** (runs-for-post before=1, after=1). Telemetry failure could not break, alter, or
fabricate the real route's result. Store restored immediately after. (3/3)

### Cleanup / restore `live-verified`
Disposable post deleted (1); all its runs deleted; a **fresh-process** re-check confirmed
`posts 127, vision_runs 0, disposable leftovers 0`. The downloaded `yolo11n-seg.pt` (gitignored)
was removed. (2/2)

## Honest scope of this verification

- Exercised live: YOLO general segmentation → real `mask_rle`, both reads, ownership 404,
  geometry round-trip, write-behind. `is_fashion=false` and YOLO succeeded, so the
  **fashion/architecture specialist passes, the Groq fine-decomposition (`decompose_fine`),
  and the vision fallback (`fallback.detect`)** were **not** exercised live this run — they
  remain `logic-tested` (P1 suite) and would surface on a fashion image / with Groq enabled /
  on a segmentation failure. A single disposable post was used; no corpus-wide activity.
- Still **not** claimed: any UI/rendered surface (no frontend in P2.0).

## Conclusion

P1 + P1.1 are now **live-verified** end-to-end on the real backend: runs persist and retrieve,
events correspond to real execution, authoritative geometry round-trips unchanged, and
telemetry is genuinely non-load-bearing. **The P2.0 gate is met.** A subsequent P2 (Passage
Rail read-only, or sibling-route instrumentation, or the `pct_` guard) may proceed on
approval; this pass built none of them and merged nothing.
