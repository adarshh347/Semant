# P2.1 — Live Sibling-Route Verification

One disposable **fashion-domain** post, exercised live against the real `visualDictionaryDB`
through the real instrumented routes (short-lived in-process; no long server). **12/12 checks
PASS; DB restored to baseline.** Branch `feat/circulation-spine-001-p1`.

Evidence: `evidence/p2.1-live-evidence.json` + `evidence/p2.1-live-script.py` (re-runnable).
Tags: `live-verified` · `blocked` (SAM2).

## Subject

Disposable post seeded with `domain={label:"fashion", is_fashion:true}`, then dissected by the
real route — the **garment segmenter ran** (`source = "segformer_clothes+yolo"`, 4 regions:
`seg_0, fseg_0..2`). Fashion-domain confirmed. Deleted at the end.

## Three families, real outcomes

| Family | Route | Live outcome | Run |
|---|---|---|---|
| **semantic** | `semantic-read` | **succeeded** — VLM ran, 4 assertions; 6 stages | `6a5ecb78…` recorded, retrievable |
| **retrieval** | `find-similar` | **succeeded** — `space=visual_identity`, **5 real neighbours** (DINOv2 over 98 corpus embeddings); 6 stages | `6a5ecb82…` recorded, retrievable |
| **geometry** | `refine-region/confirm` | **HTTP 503 — SAM2 not deployed here** → correctly minted **no run** (rejections stay run-free) | none (honest gap) |

Semantic run carried `requested_profile.region_ids=['seg_0','fseg_1','fseg_0','fseg_2']`;
find-similar carried `requested_profile.region_id='seg_0'`, `result_summary={space:
visual_identity, neighbours:5, status:ready}` — the honest correlation refs analysed in
`P2.1-correlation-investigation.md`.

## Checks (12/12)

- **setup** — fashion dissect seeded 4 regions live.
- **semantic.run_created** / **retrieval.run_created** — real runs persisted in Mongo,
  succeeded, correct operation.
- **geometry.run_or_honest_503** — SAM2 unavailable → 503, no run (design-correct).
- **retrieval_ep.{semantic_read,find_similar}** — `GET /vision-runs/{run_id}` (6 events each,
  correct `operation`) **and** `GET /vision-runs/latest?operation=…` returned each run.
- **retrieval_ep.refine** — no run to fetch (honest, given the 503).
- **retrieval_ep.ownership_404** — a run fetched under the wrong post → HTTP 404.
- **telemetry_isolation.run_id_null_or_route_survived** — semantic-read with an intentionally
  failing telemetry store returned `run_id: null` and still succeeded.
- **telemetry_isolation.no_run_doc_created** — run count for the post unchanged (3 → 3) under
  the failure.
- **cleanup.post_deleted** + **cleanup.db_restored_to_baseline** — disposable post, its runs,
  and the on-demand embedding all removed; **posts 127→127, vision_runs 0→0, region_embeddings
  98→98**; fresh-process re-check: 0 leftovers.

## Honest scope / blockers

- **Geometry (refine) not live-exercised** — SAM2 (`refine_session`) is not deployed in this
  environment, so the route returns 503 before minting a run. Its instrumentation is proven by
  unit tests (`test_refine_records_run_and_preserves_geometry_and_curator`,
  `test_refine_telemetry_failure_isolated`), not live. `blocked` (deployment, not a defect).
- Semantic + retrieval **were** live-exercised end-to-end with real models + real Mongo.
- One disposable post; no corpus-wide activity; no frontend.

## Conclusion

The P1.1 contract + write-behind model generalize cleanly to heterogeneous families:
semantic and retrieval runs are **live-verified** (persist, retrieve, telemetry-isolate,
leave geometry/curator/semantics untouched, restore); geometry is logic-verified pending SAM2
deployment. Sibling instrumentation is ready for review. **Stopping before any frontend/rail
work**, per instruction.
