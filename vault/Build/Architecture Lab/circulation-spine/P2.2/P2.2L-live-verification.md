# CIRCULATION-SPINE-001 · P2.2L — Live Verification

The rail verified **live** in the real Differential workspace against the merged backend and real
Mongo. This closes the P2.2/P2.2R gap where every state was fixture-rendered.

**Result: PASS.** Chain proven end-to-end:
`/posts/{id}` → Differential → real `DifferentialWorkspace` → real `VisionActivityRail` →
merged backend `vision-runs/latest` endpoints → real Mongo `vision_runs` documents.

Branch `feat/circulation-spine-001-p2-rail` @ `f2a52e6` (code unchanged during this gate — the
only production edit was **none**; this is verification only). Tags: `live-verified`.

## Environment

- Backend: `uvicorn backend.main:app` on `127.0.0.1:5008`, env loaded from the trusted
  out-of-worktree `.env` (**never copied into the worktree, never printed, never committed**).
  The backend `.env` contains **no `API_KEY`**, and `require_api_key` is conditional
  (`backend/security.py`: "When API_KEY is unset, all requests pass"), so no key was needed.
- Frontend: `vite build` + `vite preview` on `127.0.0.1:4173` (static; no file-watching, so the
  `ENOSPC` inotify limit that blocks `vite dev` is irrelevant).
- Servers were tracked by **exact PID** and stopped individually (no broad `pkill`).
- **The exit-144 long-lived-server blocker did not occur** — the backend ran normally.

## Subject — disposable post, real runs

A disposable post (`caption: "P2.2L DISPOSABLE …"`) carrying a real `photo_url`, dissected by the
**real instrumented route**, yielding genuine `vision_runs` documents:

| Operation | Live state | Detail |
|---|---|---|
| `dissect` | **succeeded** | `actual_source = segformer_clothes+yolo` (the garment segmenter really ran), **10 events**, measured **44716 ms** |
| `semantic_read` | **running + `stale:true`** | interrupted by a hard machine shutdown mid-run → never terminalized; the backend's stale projection flagged it — the exact residual documented in the R1 correction |
| `refine` | **absent** (`{run:null}`) | SAM2 not deployed (the UI's own "SAM · unavailable" chip agrees) → route 503s before minting a run |
| `find_similar` | **absent** (`{run:null}`) | never run |

That is a genuine live spread: **SUCCEEDED + STALE-RUNNING + two ABSENT**.

## What was verified live

| Check | Result |
|---|---|
| Rail mounts in the **real** Differential inspector | ✅ bottom of `.diff-inspector`, collapsed default "VISION ACTIVITY · 2 recorded ▸" |
| Reads real Mongo via merged endpoints | ✅ all four `latest?operation=` reads served by the live backend |
| SUCCEEDED render | ✅ "Dissect · Complete · GEOMETRY · 6 h ago", "Visual evidence produced.", `segformer_clothes+yolo · 44716 ms`, "10 stages" |
| STALE RUNNING render | ✅ "Semantic read · **Interrupted**", "This record looks interrupted — it never reported a result.", "Referenced 4 parts" |
| ABSENT render | ✅ "Refine / Find similar · No recorded activity" (quiet, not an error) |
| Does **not** obscure image/tools | ✅ image element present and **all 9 tool buttons** present with the rail expanded |
| Layout at a shorter viewport (900×640) | ✅ rail still fits inside the inspector column; no obstruction |
| Console errors | ✅ **none** |
| Runaway polling | ✅ **0** vision-run fetches in 9 s — the stale RUNNING run is correctly *not* treated as active |
| Causal wording | ✅ none in the rendered page |
| Scope statement | ✅ "Records dissect, refine, semantic read and find similar — not all vision activity." |
| **B1 — real read failure** | ✅ backend stopped → Refresh → header "**couldn’t read activity**", all four "**Couldn’t read activity**"; `saysNothingRecorded:false`, `saysNoRecordedActivity:false` |
| **B1 — recovery** | ✅ backend restarted → Refresh → real states restored ("2 recorded", Complete/Interrupted); `anyStillUnreadable:false` — transient, not a stuck error |

Screenshots: `05-live-rail-real-differential.jpg`, `06-live-shorter-viewport.jpg`,
`07-live-b1-unreadable-backend-down.jpg`.

## Review items closed / updated

- **B1 (was blocking) — CLOSED live.** A genuine backend outage renders "Couldn’t read activity",
  never "nothing recorded yet", and recovers cleanly.
- **R3 (real-layout inferred) — CLOSED.** Verified in the actual workspace: rail confined to the
  inspector grid column, image + all 9 tools unobstructed, holds at a shorter viewport.
- **R1 (wire-shape unproven) — CLOSED.** `deriveEntry` consumed the **real HTTP responses**
  (FastAPI ISO datetimes): ages rendered correctly as "6 h ago", latency `44716 ms`, 10 real
  stages, `actual_source` and `requested_profile.region_ids` ("Referenced 4 parts") all correct.
- **R2 (fetch on collapsed mount) — still open** (behaviour unchanged; 4 reads fire on mount).
- **R5 (lifecycle test labels) — partially closed:** no-runaway-polling is now *browser-observed
  live*; hook post-change-abort / unmount cleanup remain code-inferred.
- **R4, O1–O5** — unchanged, still open.

## Data safety

- All mutation confined to the **disposable post**. One incidental patch was needed on it
  (`photo_public_id`, a **required** field on the `Post` response model — its absence 500'd the
  post read; a seeding defect in my fixture, not a rail or backend defect).
- **Database restored to baseline**: `posts 127 → 127`, `vision_runs 0 → 0`,
  `region_embeddings 98 → 98`; disposable-post sweep returns empty.
- No real/user post was read-modified; no corpus expansion; no `.env` copied or committed; no
  secret printed or recorded in any log, report, or screenshot.
- Both servers stopped by exact PID; nothing left listening on `:5008` or `:4173`.

## Honest residual

- The **stale RUNNING** semantic-read run existed because a hard machine shutdown killed the
  process mid-operation — a real-world instance of the documented residual (a hard teardown can
  leave a RUNNING record; the read projection surfaces it honestly as *stale*, and the rail
  renders it as "Interrupted"). No reaper was added.
- `find_similar` and `refine` were not live-exercised as *successful* runs (find-similar was not
  re-run after the shutdown; refine needs SAM2). Their **absent** rendering is live-verified;
  their populated rendering remains fixture-verified from P2.2.
