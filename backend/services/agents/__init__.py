"""WAVE3 — situated agents: beings that inhabit a locus rather than process an image.

One agent, one locus, perceive + report. No movement (that lane is gated on the retina/density
rebuild) and no dialogue (that needs two agents). This is the seed, not the ecology.

The three modules, and why they are three:

    organs.py            what an agent perceives THROUGH — resolution, refusal, invocation
    situated_agent.py    the being itself — inhabit, perceive, record (private), report (shared)
    observation.py       what the shared ledger stores — and what it deliberately does not

The split follows the one line this wave must not blur. `situated_agent` may hold an organ's
result as `measured`, because the private/episodic view is entitled to (DECISION-measured-private-
vs-shared-ledger). `observation` stores no status at all and derives it from the mark at read
time, because the shared ledger is not. Keeping those in one module would have made the
distinction a convention; keeping them apart makes it a boundary a reader can see.
"""
