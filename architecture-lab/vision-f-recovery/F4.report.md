# VISION-F · F4 — Dependent semantic + embedding refresh — report

**Date:** 2026-07-20 · **Branch:** `feat/vision-f-recovery` · **Representative batch.**

## Order respected

Geometry was committed first (F3); F4 refreshes only the layers that *depend* on it, in order:
semantic staleness → embedding invalidation → resumable recompute.

## (1) Semantic assertions — stale only where evidence changed, decisions preserved

`geometry_recovery.stale_semantic_assertions` / `mark_stale_semantic_assertions` flag an
assertion stale ONLY when its candidate Region's mask now differs from the pre-recovery hash
(from the F1 backup); curator status / `curator_label` are never dropped (only an
`evidence_stale` marker is added, so the UX can offer a re-read).

Live: the **only** semantics-bearing post in the batch is the Pietà, which was **not**
geometry-recovered (already canonical) → **stale = []**, and its **2 curator decisions
(`arch_0`, `seg_0`) preserved**. No semantic rerun was needed or performed.

## (2)+(3) Embedding refresh — resumable, no duplicates, version-isolated

Refresh ran through the E6 resumable job (`indexing_service.run_batch`) over the 6 batch posts:

| run | result | rows | notes |
|-----|--------|------|-------|
| refresh | indexed **2**, skipped **4**, failed 0 | 97 → **97** | the 2 recovered posts re-embedded; **no duplicate rows** (deterministic-id upsert replaced stale in place); wall **12.9 s**, peak VRAM **107 MiB** |
| resume | indexed **0**, skipped **6** | 97 | idempotent — **geometry-stable posts not recomputed** |

- Recovered identity vectors now use route **`mask_pool`** (was `crop_cls` for the box-legacy
  evidence) — the refresh genuinely uses the new masks. **Space unchanged**
  (`dinov2_vits14|identity|dino-v1|384`): only geometry changed, so vectors are replaced in the
  same version space, never proliferated — **old versions stay query-isolated**.
- **Stale embeddings after refresh: 0.**

Automated (`test_vision_f4.py`, 3): unchanged evidence keeps assertions valid; a changed mask marks
only that assertion stale; marking preserves the curator's status + label. Backend suite **225
passed** (222 + 3).

**Gate result: PASS** — interrupt/resume produces no duplicates; old versions remain isolated;
geometry-stable items are not recomputed unnecessarily; semantic curator decisions are preserved
and only evidence-changed assertions would be flagged for a re-read (none in this batch).

## Measurements (for F7 planning)
Representative refresh: **2 posts / 26 regions re-embedded in ~13 s**, peak **107 MiB** VRAM, 0
failures, storage flat (rows replaced in place). This is the measured per-batch throughput to
scale the F7 expansion estimate against (not the E6 dry-run promise).

## Next
F5 — full-circulation browser acceptance across the representative fixtures (exact mask → Ground →
Percept → Mention → recall → Find similar → open source → return), and geometry-revision behaviour.
