# VISION-F · F3 — Geometry recovery + controlled dissection — report (IN PROGRESS)

**Date:** 2026-07-20 · **Branch:** `feat/vision-f-recovery` · **Scope so far: 1 post (approved narrow subset).**

## Approach

Per-Region honest recovery (`backend/services/geometry_recovery.py`), driven through the F1
ledger + backup with the F2 skip-guard:

- `retain_mask` — already authoritative; untouched.
- `derive_polygon` — a GENUINE traced polygon (≥ 8 pts) rasterized to a canonical mask.
- `sam_refine_box` — box-only evidence: SAM segments the real pixels from the box **prompt**
  (genuine, never fabricated), keeping the Region id + label + curator fields.
- `retain_box` — honest fallback when SAM is down.

`apply_mask_to_region` changes only geometry (mask/polygons/box/rev/provenance), saves the old
geometry under `_prev_geometry`, and preserves every curator field. Detached grounds are flagged
`detached=True` visibly, never deleted.

## Dry-run — representative batch (read-only, presented for approval)

| post | fixture | plan | mutates |
|------|---------|------|---------|
| `695be6c9` | five-sculpture | 1 derive + 9 SAM-refine | yes |
| `695be8ba` | fashion/garment | 7 derive + 9 SAM-refine | yes |
| `695be786` | architecture | retain 7 masks; mark 2 detached grounds | grounds only |
| `695be794` | figurative painting | retain 1 mask; mark 2 detached grounds | grounds only |
| `6a5b91ec` | abstract painting | retain 1 mask | no |
| `695be77` | Pietà | retain 3 masks; curated semantics preserved | no |

Totals: 8 polygon-derives + 18 SAM-refines + 4 detached-ground flags. The user approved a
**narrowed first subset** — execute the five-sculpture post, inspect, then decide on the rest.

## Executed — `695be6c9` (five-sculpture), committed

Through the ledger (`running → committed`), backup in place:

- **10/10 regions now carry authoritative masks** (was 0): `seg_0` person via polygon-derive; the
  9 dynastic heads/crowns (Solanki … Gandhara) via **SAM box-refine**.
- **curator-only hash preserved byte-for-byte** (`03fdc725…` → `03fdc725…`) — every Region id,
  label ("Solanki crown" …), note, priority and weight survived; only geometry changed.
- **Region ids preserved, labels preserved**; `geometry_rev` bumped to 1; old geometry saved.
- **No slivers, nothing out-of-frame** (registration holds — each mask sits on its labeled head;
  evidence: `F3-evidence/sculpture_recovered_masks.png`).
- **10 embeddings now stale** (geometry changed) → queued for the F4 refresh.
- Persistence **survived a mid-session power loss** — the commit was durable; on restart the DB
  showed 10/10 masks and the ledger `committed`.

Automated (`test_vision_f3.py`, 7): classification (mask/polygon/box; a 4-pt "polygon" is a box);
honest action choice; SAM-down → retain_box (never fabricate); derive/apply preserve id + label +
curator fields + save old geometry; detached grounds marked not deleted; `recover_post` preserves
the curator-only hash across a geometry change. Backend suite **222 passed**.

**Gate (partial): PASS for the executed subset** — no slivers / anchor prison, source-to-mask
registration holds, curator hashes/identities survive. **Remaining representative posts + browser
review pending user go-ahead.**

## Next (pending approval)
Complete the representative batch — `695be8ba` (fashion: 7 derive + 9 SAM), detached-ground marking
on `695be786`/`695be794` — then F5 browser review before any expansion.
